const PATH_PREFIX = "~";
const COMPONENTS = "components";
const TYPES = "types";
const STYLES = "styles";
const DEFINITIONS = "definitions";
const HOOKS = "hooks";
const LIBS = "libs";
const PAGES = "pages";
const UTILS = "utils";

module.exports = {
    paths: {
        COMPONENTS,
        STYLES,
        DEFINITIONS,
        HOOKS,
        LIBS,
        TYPES,
        PAGES,
        UTILS
    },
    PATH_PREFIX
};